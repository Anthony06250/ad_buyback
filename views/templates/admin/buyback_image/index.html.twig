{% extends '@PrestaShop/Admin/layout.html.twig' %}

{% block content %}
    {% include '@PrestaShop/Admin/Common/Grid/grid_panel.html.twig' with {'grid': grid} %}

    <div class="modal fade" id="buybackImage-grid-view-modal" aria-modal="true">
        <div class="modal-dialog">
            <div class="modal-content">
                <div class="modal-header">
                    <h4 class="modal-title">
                        {{ 'Image preview' | trans({}, 'Modules.Adbuyback.Admin') }}
                    </h4>
                    <button class="close" type="button" data-dismiss="modal">×</button>
                </div>
                <div class="modal-body text-left font-weight-normal row mt-3">
                    <article style="float:none; margin: 0 auto;">
                        <figure class="ad-bb-modal-figure">
                            <img src="" alt="">
                            <figcaption class="text-center mt-3"></figcaption>
                        </figure>
                    </article>
                </div>
                <div class="modal-footer mt-3">
                    <button type="button" class="btn btn-outline-secondary btn-lg" data-dismiss="modal">
                        {{ 'Close' | trans({}, 'Modules.Adbuyback.Admin') }}
                    </button>
                    <a href="" class="ad-bb-modal-view btn btn-info btn-lg" target="_blank">
                        {{ 'Open in new tab' | trans({}, 'Modules.Adbuyback.Admin') }}
                    </a>
                </div>
            </div>
        </div>
    </div>
{% endblock %}

{% block javascripts %}
    {{ parent() }}

    <script src="{{ asset('/modules/ad_buyback/views/js/buyback_image.admin.index.bundle.js') }}"></script>
    <script src="{{ asset('/admin-dev/themes/default/js/bundle/pagination.js') }}"></script>
{% endblock %}
